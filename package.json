{
  "name": "cs554-rhythm",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "setup": "npm i && cd web && npm install && cd ../api && npm install",
    "seed": "cd api && npm run seed",
    "start": "concurrently \"npm run --prefix api start\" \"npm run --prefix web start\"",
    "dev": "concurrently \"npm run --prefix api dev\" \"npm run --prefix web dev\"",
    "format": "prettier -w ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "prettier": "^3.2.5"
  },
  "dependencies": {
    "@emotion/react": "^11.11.4",
    "react-spinners": "^0.13.8"
  }
}
